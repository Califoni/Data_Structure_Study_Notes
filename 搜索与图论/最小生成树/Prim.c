/*
算法基本思想：
	对于图G=(V,E),令T=(U,TE)为其最小生成树（初始为空）
	初始化：
		先从V中任意取一个点v0加入U中，此时U={v0},TE=NULL
	循环操作：
		寻找U中所有点的关联边，保留其中若干边，被保留的边必须具备该性质：
			可知每条边有两个关联顶点，易知上述所有边的一个关联顶点属于集合U,则被保留的边的另一个顶点必定属于集合(V-U)
		然后在被保留的边中寻找权值最小的将其加入TE，关联的另一个顶点加入U
	当U=V时结束循环
*/

